# Dev only: the panel UI runs on a different port than the API, so point the
# terminal WebSocket directly at the backend. In production behind the bundled
# nginx the terminal uses the same origin and this is not needed.
NEXT_PUBLIC_BACKEND_WS=ws://localhost:8000
